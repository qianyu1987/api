# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

普通用户通过统一 API Key 使用文本、图片和视频能力，并管理钱包、套餐与用量。管理员负责渠道、价格、利润、账务、媒体内容和平台风险管理。

## Product Purpose

GPT TOKEN 提供 OpenAI 兼容的统一 AI 接入、钱包与套餐计费、用量查询，以及图片和视频创作。成功意味着用户能从注册充值到生成和查账完成自助闭环，管理员能控制内容、成本与利润。

## Positioning

平台用一个账户、钱包和控制台连接多个上游，并在用户看不到上游实现细节的前提下提供故障切换、价格快照和统一账务。

## Operating Context

用户主要在桌面或手机浏览器操作控制台，也会在 CC Switch 等 OpenAI 兼容客户端中使用 API。管理员以桌面后台进行高密度运营。

## Capabilities and Constraints

- 原生 HTML、CSS、JavaScript 前端，不迁移框架。
- 用户侧隐藏上游渠道、真实模型、成本和利润。
- 图片与视频作品默认私密，只有管理员审核发布后进入广场。
- 管理操作不得破坏历史账单、成本快照和生成记录。
- 金额以数据库微元整数为账务来源，平台目标最低毛利率为 30%。
- 钱包充值当前为支付 1 元到账 3 元；月套餐独立结算。

## Brand Commitments

产品名为 GPT TOKEN，保留现有 Logo。中文界面使用清楚、直接、可操作的文案。

## Evidence on Hand

现有生产系统已包含登录注册、充值、套餐、API Key、用量、利润后台、渠道故障切换和媒体生成。不得虚构在线率、用户规模或客户背书。

## Product Principles

- 用户先理解结果、费用和下一步操作。
- 内容默认私密，公开必须经过管理员审核。
- 用户界面不泄露供应商和真实模型实现。
- 账务和历史记录可追溯且不可被内容管理破坏。
- 手机端完成用户闭环，复杂运营集中在桌面后台。
